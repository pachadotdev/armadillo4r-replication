% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bp_figures.R
\name{bp_prepare_data}
\alias{bp_prepare_data}
\title{Prepare Data for Baumeister-Peersman (2013) Analysis}
\usage{
bp_prepare_data(
  data = NULL,
  L = 4,
  training_years = 25,
  n_burn = 50000,
  n_draws = 5000,
  thinning = 10,
  stability_constraint = 0,
  lambda_param = 1e-04,
  first_diff = 1
)
}
\arguments{
\item{data}{Data matrix with columns qo (oil production), po (real oil price),
and y (global activity). If NULL, uses bp_bench dataset.}

\item{L}{Lag order (default 4 for quarterly data).}

\item{training_years}{Years for training sample (default 25).}

\item{n_burn}{Number of burn-in iterations (default 50000).}

\item{n_draws}{Number of draws from Gibbs sampler (default 5000).}

\item{thinning}{Thinning interval (default 10).}

\item{stability_constraint}{Whether to impose VAR stability (default 0).}

\item{lambda_param}{Time variation parameter (default 0.0001 = Primiceri "Small").}

\item{first_diff}{Whether data is in first differences (default 1).}
}
\value{
A list containing:
  \describe{
    \item{y}{Prepared data matrix}
    \item{tvp_result}{TVP-VAR estimation results}
    \item{bp_theme}{Common ggplot theme for consistency}
  }
}
\description{
Prepares data matrix and estimates TVP-VAR model for replication.
}
